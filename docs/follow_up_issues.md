# フォローアップ課題 / Follow-up Issues

このリポジトリで実施・検討すべきフォローアップの一覧です。優先度順に並べています。

## 1) ドキュメント化と Issue 作成
- `src/ai.ts` と `src/input.ts` の変更点（Worker 外部化、inline Blob フォールバック、DAS/ARR のデフォルト調整）を `docs/` に短くまとめる。必要なら個別の GitHub Issue を作成する。

## 2) CI 統合
- `scripts/test-ci.js` と `scripts/auto-test-ci.js` を CI ワークフローに組み込み、プルリク時に単発テスト（`npm run test:ci`）と短時間の自動キャプチャ（`npm run auto-test:ci`）を実行する。

## 3) `recordings/` の扱い
- `recordings/*.json` は外部アーティファクトに切り出すか `.gitignore` に追加してリポジトリ肥大化を避ける。過去の大きなファイルはアーカイブ方針を決める。

## 4) ランタイム検証と追試
- `node scripts/debug_capture_one_run.js` と `node scripts/auto_test_early_fallback.js` を使い、ブラウザ上で Worker の外部ロード・inline Blob フォールバック・早期フォールバックの挙動を再現・記録する。ログは `recordings/` に保存する（既に短時間分が保存済み）。

## 5) auto-sweep（DEBOUNCE）拡張
- `runParameterSweep()` / `probeStateWithParams()` の DEBOUNCE ロジック仕様を決め、ユニットまたは統合テストで回帰を防ぐ。優先度: 中。

## 6) テスト整備
- DAS/ARR、early-fallback、worker-overwrite シナリオの統合テストを追加して回帰検知を強化する。

## 7) コード品質・リファクタ
- `worker_planner.js` を最小限の外部ファイルとして維持し、`src/ai.ts` には大きなテンプレート文字列を含めない方針を明記する。

## 8) 次アクション（推奨）
1. このファイルをコミットしてプッシュする（完了済みなら PR に紐付け）。
2. `recordings/` の扱い方をチームで合意し、必要なら過去ファイルを削除して再コミットする。
3. CI ワークフローの更新（`/.github/workflows/ci.yml`）を準備する。

---
Generated by automation to track follow-up work after AI/worker refactor and test fixes.
