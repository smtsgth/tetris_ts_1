import Input from './input.js';

// Minimal UI wiring stub. Keeps file DOM-free for CI environments.
export default function attachInputSettings(input: Input) {
  // no-op: preserve API surface for `src/main.ts`
  return;
}
